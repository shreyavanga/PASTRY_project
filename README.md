# PASTRY_project

run code : g++ s.cpp -lcrypto -pthread

command:

    port <port_no>
    
    join <ip> <port>
    
    print                         //print for whole table
    
    put <key> <value>
    
    get <key>
    
